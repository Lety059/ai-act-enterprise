services:
  - type: web
    name: ai-act-enterprise
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && python tools/compile_kb.py
    startCommand: streamlit run app.py --server.port $PORT --server.address 0.0.0.0
